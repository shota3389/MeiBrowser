<UserControl x:Class="GUI.Views.FileTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:GUI"
             xmlns:loc="clr-namespace:GUI"
             mc:Ignorable="d"
             Background="{DynamicResource WindowBackgroundBrush}">

    <Grid>
        <Grid Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- package summary and the download action for this tab -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="DownloadButton" Content="{loc:Loc Files_DownloadSelected}"
                        Width="150" Height="30" Click="DownloadButton_Click"/>

                <TextBlock Grid.Column="1" x:Name="PackageSummary" Margin="12,0,0,0"
                           VerticalAlignment="Center" FontSize="12"
                           TextTrimming="CharacterEllipsis"
                           Foreground="{DynamicResource ControlDisabledForegroundBrush}"/>
            </Grid>

            <!-- search, regex and the filter, all scoped to this tab -->
            <Grid Grid.Row="1" Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <TextBox x:Name="SearchBox"
                             Height="30"
                             Background="{DynamicResource ControlBackgroundBrush}"
                             Foreground="{DynamicResource WindowForegroundBrush}"
                             CaretBrush="{DynamicResource WindowForegroundBrush}"
                             BorderBrush="{DynamicResource ControlBorderBrush}"
                             VerticalContentAlignment="Center"
                             Padding="5,0"
                             TextChanged="SearchBox_TextChanged"
                             KeyDown="SearchBox_KeyDown"/>
                    <TextBlock Text="{loc:Loc Files_SearchHint}"
                               Foreground="{DynamicResource ControlDisabledForegroundBrush}"
                               IsHitTestVisible="False"
                               VerticalAlignment="Center" Margin="7,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <CheckBox Grid.Column="1" x:Name="RegexToggle" Content="{loc:Loc Files_Regex}"
                          VerticalAlignment="Center" Margin="10,0,0,0"
                          Foreground="{DynamicResource WindowForegroundBrush}"
                          ToolTip="{loc:Loc Files_RegexTooltip}"/>

                <Button Grid.Column="2" x:Name="FilterButton" Content="{loc:Loc Files_Filter}" Width="90" Height="30"
                        Margin="10,0,0,0" Click="FilterButton_Click"
                        ToolTip="{loc:Loc Filter_DescribeNone}"/>

                <Button Grid.Column="3" x:Name="SearchButton" Content="{loc:Loc Files_Search}" Width="90" Height="30"
                        Margin="6,0,0,0" Click="SearchButton_Click"/>
            </Grid>

            <!-- Virtualisation for rendering many items fast -->
            <TreeView Background="{DynamicResource ControlBackgroundBrush}" Name="FileTree" Grid.Row="2"
                      ItemsSource="{Binding RootItems}"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True"
                      PreviewMouseLeftButtonDown="FileTree_PreviewMouseLeftButtonDown">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White"/>
                    <HierarchicalDataTemplate DataType="{x:Type local:FileItem}" ItemsSource="{Binding Children}">
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="#1A5CA8"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Image Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding Name}" Width="400"/>
                            <TextBlock Text="{Binding Size}" Width="80" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding Type}" Width="100" HorizontalAlignment="Right"/>
                            <TextBlock Text="{Binding Elements}" Width="100" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>

        <Grid x:Name="LoadingOverlay" Background="#80000000" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="LoadingText" Text="{loc:Loc Common_Loading}" Foreground="White" FontSize="16"
                           HorizontalAlignment="Center"/>
                <ProgressBar IsIndeterminate="True" Width="200" Height="16" Margin="0,8,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
